{
  "name": "routed-monitoring-hub-worker",
  "private": true,
  "version": "1.0.8",
  "description": "Cloudflare Worker deployment of the Routed Monitoring hub.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "secret:put": "wrangler secret put TOKEN_MONITOR_SECRET"
  },
  "cloudflare": {
    "bindings": {
      "TOKEN_MONITOR_SECRET": {
        "description": "Required. Shared secret for protected hub routes. Agents, widgets, and API clients must present it when posting or reading private stats. Generate a long random string, e.g. `openssl rand -hex 32`."
      },
      "PUBLIC_STATS_ENABLED": {
        "description": "Optional. Set to 1 to expose the unauthenticated GET /api/public/stats endpoint. Leave blank to keep it off."
      }
    }
  },
  "author": "Javis",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Javis603/routed-monitoring.git",
    "directory": "worker"
  },
  "homepage": "https://github.com/Javis603/routed-monitoring/tree/main/worker#readme",
  "bugs": {
    "url": "https://github.com/Javis603/routed-monitoring/issues"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
